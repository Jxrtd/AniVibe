<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".Settings_HelpAndSupportAcitivity"
    android:orientation="vertical"
    android:padding="24dp"
    android:background="#1A1A1D">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:padding="8dp">

            <!-- 1) Arrow at left -->
            <ImageButton
                android:id="@+id/devbackbutton"
                android:layout_width="30dp"
                android:layout_height="30dp"
                android:src="@drawable/arrow_left_icon"
                android:scaleType="fitCenter"
                android:background="?attr/selectableItemBackgroundBorderless"
                app:tint="@color/white" />

            <!-- 2) Title stretches to fill remaining space -->
            <TextView
                android:id="@+id/title"
                android:fontFamily="sans-serif-condensed"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Help and Support"
                android:textSize="25sp"
                android:textColor="#FFFFFF"
                android:textStyle="bold"
                android:gravity="center"/>

            <!-- 3) Invisible spacer same width as arrow -->
            <View
                android:layout_width="30dp"
                android:layout_height="0dp"/>
        </LinearLayout>





        <!-- Enhanced ListView with divider and padding -->
        <ListView
            android:id="@+id/lvHelpAndSupport"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="#232326"
            android:divider="#2E2E32"
            android:dividerHeight="1dp"
            android:paddingVertical="8dp"
            android:clipToPadding="false"
            android:scrollbarStyle="outsideOverlay"/>
    </LinearLayout>
</LinearLayout>

